<!DOCTYPE html>
<html lang="en" dir="ltr">
  <center>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%= erb(:textarea, :locals => { :object => "new-message", :content => nil, :value => nil }) %>
    <% @message.reverse.each do |message| %>
      <br>
      <div>
      <a href = "/messages/<%=message.id%>"> <%= message.display %> </a>
      </div>
        <%= message.time %>
        <br>
    <% end %>
  </body>
  </center>
</html>
